import { Text } from "react-native";
import { View } from "react-native";

export const SobreScreen = ({ navigation }) => {
  return (
    <View>
      <Text>eus ou o sobre</Text>
    </View>
  );
};
